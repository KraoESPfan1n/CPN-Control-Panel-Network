[package]
name = "cpn-installer"
version = "0.2.6-alpha.29"
edition = "2024"
description = "CPN Server Panel installer (web UI and SSH/CLI)"
license = "GPL-3.0-only"
repository = "https://github.com/Control-Panel-Network/CPN-Control-Panel-Network"

[lib]
name = "cpn_installer"
path = "src/lib.rs"

[[bin]]
name = "cpn-installer"
path = "src/main.rs"

[[bin]]
name = "cpn"
path = "src/bin/cpn.rs"

[dependencies]
actix-web = "4"
actix-ws = "0.3"
aes-gcm = "0.11"
base64 = "0.23.1"
clap = { version = "4", features = ["derive"] }
data-encoding = "2"
futures-util = "0.3"
hmac = "0.13"
lettre = { version = "0.11", default-features = false, features = ["builder", "smtp-transport", "native-tls"] }
pbkdf2 = { version = "0.13", default-features = false, features = ["hmac"] }
qrcode = { version = "0.14", default-features = false, features = ["svg"] }
rand = "0.9"
rpassword = "7"
rust-embed = "8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha1 = "0.11"
sha2 = "0.11"
tokio = { version = "1", features = ["io-util", "macros", "net", "process", "rt-multi-thread", "signal", "sync", "time"] }
url = "2"
uuid = { version = "1", features = ["serde", "v4", "v5"] }
webauthn-rs = { version = "0.5", default-features = false, features = ["conditional-ui", "danger-allow-state-serialisation"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
panic = "abort"
